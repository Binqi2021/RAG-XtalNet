# GitHub 上传工作流程

## ⚡ 快速上传（三步）

```bash
cd /public/home/huangtianqi/XtalNet

# 1. 添加所有改动
git add .

# 2. 提交（替换为你的描述）
git commit -m "描述你的改动"

# 3. 推送到GitHub
git push origin main
```

---

## 📋 详细步骤

### 1. 查看改动
```bash
git status              # 查看改动状态
git diff                # 查看具体改动内容
```

### 2. 添加改动
```bash
git add .               # 添加所有改动
git add file.py         # 只添加特定文件
```

### 3. 提交改动
```bash
git commit -m "描述你的改动"
```

### 4. 推送到GitHub
```bash
git push origin main
```

---

## 💡 提交信息示例

```bash
git commit -m "Clean up project: remove temp files and organize scripts"
git commit -m "Add RAG retrieval system"
git commit -m "Fix data path configuration"
git commit -m "Update README with retrieval instructions"
```

---

## ⚠️ 首次设置（只需一次）

如果还没连接GitHub仓库：

```bash
# 1. 在GitHub创建新仓库（不要初始化README）

# 2. 添加远程仓库
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 3. 推送到GitHub
git branch -M main
git push -u origin main
```

---

## 🔍 常用命令

### 查看状态
```bash
git status              # 查看改动状态
git status --short      # 简洁模式
git diff                # 查看未暂存的改动
git diff --staged       # 查看已暂存的改动
```

### 撤销操作
```bash
# 撤销未暂存的改动
git checkout -- file.py

# 撤销已暂存的改动（保留文件）
git reset HEAD file.py

# 撤销最后一次提交（保留改动）
git reset --soft HEAD~1
```

### 查看历史
```bash
git log                 # 查看提交历史
git log --oneline       # 简洁模式
```

---

## 💡 提交信息规范

建议使用清晰的提交信息：

```
格式：<类型>: <简短描述>

类型：
- feat: 新功能
- fix: 修复bug
- docs: 文档更新
- refactor: 代码重构
- style: 格式调整
- test: 测试相关
- chore: 其他改动

示例：
git commit -m "feat: Add retrieval system for RAG"
git commit -m "fix: Correct data path in eval_utils"
git commit -m "docs: Update README with setup instructions"
```

---

## 🚀 快速上传（一键）

创建别名（可选）：
```bash
# 添加到 ~/.bashrc
alias gup='git add . && git commit -m "Update" && git push origin main'
```

然后每次只需：
```bash
gup
```

---

## ⚡ 当前改动快速上传

```bash
cd /public/home/huangtianqi/XtalNet
git add .
git commit -m "Clean up project structure"
git push origin main
```

